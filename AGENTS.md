# 训练端上下文规则

## 文档门禁与状态同步

开始任何训练、数据、模型、部署或闭环任务时，第一条动作必须完整读取三份 `WORKSPACE_CONTEXT.md`；读取前不得编辑、训练、部署、启动进程或清理目录。对话摘要、旧日志和文件名不能替代文档与现场核验。

每个任务只保留一个当前唯一目标和一个下一步唯一动作。任务开始、阶段完成、阻塞、暂停/上下文压缩前和结束时，立即更新三份交接文档；无变化的端也要注明无变化。状态使用“未开始/进行中/阻塞/已验收”，证据标记为现场验证、静态推断、历史记录或未验证。

开始任何训练、数据、模型、部署或闭环相关工作前，必须先阅读：

1. `D:\asv-vla-training\WORKSPACE_CONTEXT.md`
2. `D:\asv-unreal-simulation\WORKSPACE_CONTEXT.md`
3. `/home/jetson/jetson_asv_ws/WORKSPACE_CONTEXT.md`

`WORKSPACE_CONTEXT.md` 是训练端当前状态的唯一事实入口。不要用对话摘要、旧实验目录或文件名推断当前模型和数据。

完成采集、训练、评估、模型替换、部署、清理或路径变更后，必须立即更新交接文档，记录目标、进度、证据、哈希、阻塞项和下一步。所有事实必须标记为已验证、历史或未验证。三份文档冲突时先暂停并核对现场。
